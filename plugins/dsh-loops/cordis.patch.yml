# The dsh-loops bundle patch: mounts the loop plugin, whose client half owns
# the Loops settings section (loop roster, run status, file editor) over the
# shared settings.section seat.

- insert:
    - id: loops
      name: 'dsh-loops'
      config: {}
