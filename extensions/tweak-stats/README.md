# tweak-stats

Session observability: the tweaks-stats settings section and the dsh stats/dsh sessions verbs over the projection cache.

An extension of the `tweaks` settings surface, split out of the bundled `tweaks` package.
