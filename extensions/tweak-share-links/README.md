# tweak-share-links

Self-hosted read-only session share links: the share route, share settings section, and the dsh share verb.

An extension of the `tweaks` settings surface, split out of the bundled `tweaks` package.
