# skin-settings

Settings UI for selecting the active Stack skin.

The picker is not a settings section of its own: it registers into
`settings.appearance.tab` (see `@dsh-stack/settings-panel`) and renders as the
**Skins** page of the Appearance section, beside the theme studio, icons and
themes.
