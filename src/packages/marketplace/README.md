# marketplace

Marketplace-source registry abstraction for DSH Stack: the plugin defines the
`MarketplaceSource` contract and the `MarketplaceRegistry` aggregation
service that concrete source extensions (e.g.
`@dsh-stack/marketplace-source-dsh-stack`) plug their catalog into.
