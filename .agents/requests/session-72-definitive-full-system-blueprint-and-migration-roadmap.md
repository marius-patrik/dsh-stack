# Session 72 Request: Definitive Full-System Blueprint & Step-by-Step Migration Roadmap

## User Directives (Verbatim)
`/plan map it out properly`

## Blueprint Scope
1. **Full Physical & Logical Layout**: Complete filesystem directory hierarchy with every package, index.ts, check-plugin.mjs, and package.json.
2. **Inter-Plugin Dependency DAG & Injection Matrix**: Exact graph of all hard (`inject`), optional (`optional`), and exported (`ctx.*`) services.
3. **Component Code Migration Mapping**: Explicit line-range decomposition of `dsh-providers/client.js` and `dsh-tweaks/client.js` into their new single-responsibility packages.
4. **Interactive Data & Event Pipelines**: Chat prompt, tool call, PTY terminal stream, and LSP diagnostic data flows.
5. **Phase-by-Phase Execution Roadmap**: Concrete execution steps with verification gates.
