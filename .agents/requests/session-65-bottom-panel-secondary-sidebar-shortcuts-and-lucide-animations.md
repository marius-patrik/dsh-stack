# Session 65 Request: Panel Accessibility, Global Shortcuts, and Lucide Animations

## User Directives

1. **Terminal Placement (`terminall is still in wrong are`)**:
   - Ensure terminals open in the dedicated **Bottom Panel** by default instead of mounting as an occupant over the top chat area unless explicitly chosen.

2. **Always Accessible Panels (`bottom panel and secondary sidebar should always be accesible - they have to be like glitched through to currently by openin g something new`)**:
   - Add persistent toggle buttons in the top navigation/tab bar for the Bottom Panel and Secondary Sidebar.
   - Secondary Sidebar and Bottom Panel must be accessible and toggleable anytime even when empty (with a clean quick-actions surface).

3. **Global Keyboard Shortcuts**:
   - `Cmd+J` (macOS) / `Ctrl+J` (Windows/Linux): Toggle Bottom Panel open / collapsed.
   - `Cmd+Option+B` (macOS) / `Ctrl+Alt+B` (Windows/Linux): Toggle Secondary Sidebar open / collapsed.
   - `Cmd+Shift+P` (macOS) / `Ctrl+Shift+P` (Windows/Linux): Trigger / focus the search input inside the left sidebar.

4. **Authentic Lucide Animated Vectors & Transitions (`replace actually all icons with the ones from the real lucide-animate package and make them all use the animations from the package`)**:
   - Add authentic `@keyframes lucide-...` animations for search, terminal slide, plus spin, cw rotate, trash lid open, pencil tilt, pin drop, gear turn, chat bounce, folder open, sparkles pulse, dock slide, panel bottom, copy slide, eye wink, mic pulse, and restore bounce.
