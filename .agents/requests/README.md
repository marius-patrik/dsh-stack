# Requests

Every user prompt that contains work instructions becomes a request file here.

Format: `<YYYY-MM-DD-HHMM>-<slug>.md`

Each file contains the raw user prompt verbatim plus immediate context.
Requests are the immutable record of what was asked for.
