# tweak-keybinds

Keybind settings surface: the tweaks-keybinds section schema and validation consumed by the client.

An extension of the `tweaks` settings surface, split out of the bundled `tweaks` package.
