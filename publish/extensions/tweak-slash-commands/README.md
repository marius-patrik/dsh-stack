# tweak-slash-commands

Config-file slash commands: a settings-driven registry bridging into the harness command registry.

An extension of the `tweaks` settings surface, split out of the bundled `tweaks` package.
