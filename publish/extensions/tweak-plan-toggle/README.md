# tweak-plan-toggle

Plan/Build toggle: registers the /build command leaving plan mode through the harness plan-mode controller.

An extension of the `tweaks` settings surface, split out of the bundled `tweaks` package.
