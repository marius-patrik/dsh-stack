# tweak-drag-drop

Image drag-drop settings surface: the tweaks-drag-drop section (enable + max image bytes) consumed by the client attachment seam.

An extension of the `tweaks` settings surface, split out of the bundled `tweaks` package.
