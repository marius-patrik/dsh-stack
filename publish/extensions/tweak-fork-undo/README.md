# tweak-fork-undo

Fork-based session undo/redo: /undo and /redo fork a new session from an earlier logged boundary.

An extension of the `tweaks` settings surface, split out of the bundled `tweaks` package.
