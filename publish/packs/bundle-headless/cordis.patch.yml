# GENERATED FILE -- do not hand-edit.
#
# Produced by `node src/scripts/generate-stack-bundle-headless-patch.mjs write`
# from publish/packs/bundle/cordis.patch.yml's own package list, filtered to
# packages whose built `inject` array names neither `webServer` nor `loader` --
# the two services a headless boot never composes (dsh-stack#213). Regenerate
# after regenerating the full bundle; `pnpm --filter @dsh-stack/pack-bundle-headless
# run verify` fails loud on drift.
#
# Each row's `name` is a PACKAGE NAME resolved through the profile's own
# node_modules, not a filesystem path. Row `id`s are derived from the package
# name so a later `dsh plugin` inspection can trace a mounted row back to its
# source.
- insert:
    - id: agent-loops
      name: '@dsh-stack/agent-loops'
    - id: agent-tools
      name: '@dsh-stack/agent-tools'
    - id: agents
      name: '@dsh-stack/agents'
    - id: credential-vault
      name: '@dsh-stack/credential-vault'
    - id: dialects
      name: '@dsh-stack/dialects'
    - id: formatters
      name: '@dsh-stack/formatters'
    - id: icon-engine
      name: '@dsh-stack/icon-engine'
    - id: lsp
      name: '@dsh-stack/lsp'
    - id: marketplace
      name: '@dsh-stack/marketplace'
    - id: marketplace-source-dsh-stack
      name: '@dsh-stack/marketplace-source-dsh-stack'
    - id: profile-ui
      name: '@dsh-stack/profile-ui'
    - id: provider-rotation
      name: '@dsh-stack/provider-rotation'
    - id: providers
      name: '@dsh-stack/providers'
    - id: repos
      name: '@dsh-stack/repos'
    - id: sidebar-preferences
      name: '@dsh-stack/sidebar-preferences'
    - id: sidebar-settings
      name: '@dsh-stack/sidebar-settings'
    - id: sidebar-shell
      name: '@dsh-stack/sidebar-shell'
    - id: skin-host
      name: '@dsh-stack/skin-host'
    - id: skin-runtime
      name: '@dsh-stack/skin-runtime'
    - id: skin-settings
      name: '@dsh-stack/skin-settings'
    - id: themes
      name: '@dsh-stack/themes'
    - id: translator
      name: '@dsh-stack/translator'
    - id: tweak-drag-drop
      name: '@dsh-stack/tweak-drag-drop'
    - id: tweak-fork-undo
      name: '@dsh-stack/tweak-fork-undo'
    - id: tweak-keybinds
      name: '@dsh-stack/tweak-keybinds'
    - id: tweak-plan-toggle
      name: '@dsh-stack/tweak-plan-toggle'
    - id: tweak-share-links
      name: '@dsh-stack/tweak-share-links'
    - id: tweak-slash-commands
      name: '@dsh-stack/tweak-slash-commands'
    - id: tweak-stats
      name: '@dsh-stack/tweak-stats'
    - id: tweaks
      name: '@dsh-stack/tweaks'
