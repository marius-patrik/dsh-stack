# dependencies
node_modules/
.hub/
.vite/

# harness build output
harness/apps/*/lib/
harness/packages/*/lib/
harness/*/dist/

# state (never commit) — .agents/ in repo is docs + hooks (tracked);
# ~/.agents is the harness home (state folder, never committed)
.agents/*
!.agents/*.md
!.agents/hooks/
!.agents/hooks/**
!.agents/rules/
!.agents/rules/**
!.agents/requests/
!.agents/requests/**
.dsh/
.credentials.yaml
accounts.vault

# secrets / browser artifacts
*.vscdb
*cookies*
*.pem

# editor
.DS_Store
